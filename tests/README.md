# Test Suites

- API tests: Jest + Supertest (`tests/api`).
- Frontend e2e tests: Playwright (`tests/e2e`).

Run all suites:

```bash
npm test
```

Run only API tests:

```bash
npm run test:api
```

Run only frontend e2e tests:

```bash
npm run test:e2e
```
